services:
  - type: web
    name: orbitalguard-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    envVars:
      - key: ALLOWED_ORIGINS
        value: "*"
